
class PropertiesUtils {

  /// Method to proceed layer property for native
  static dynamic processProperty(dynamic property) {

    // if (property == null || property is String || property is int || property is doub ) {
    //
    // }
  }
}